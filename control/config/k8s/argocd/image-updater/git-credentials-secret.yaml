# =========================
# Git 写回凭证 Secret
# =========================
# ArgoCD Image Updater 需要写回 Git 仓库来更新 image tags
# 因此需要 Git 仓库的写权限
#
# 注意：ConfigMap 配置已移至 Helm values.yaml
# 此文件仅包含 Git credentials Secret

apiVersion: v1
kind: Secret
metadata:
  name: git-credentials
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-image-updater
type: Opaque
stringData:
  # 使用用户名和 Token
  username: Johnny-dai-git
  password: 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

