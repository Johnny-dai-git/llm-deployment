apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cmd-params-cm
    app.kubernetes.io/part-of: argocd
data:
  # 关闭 HTTPS 强制，使用 HTTP
  server.insecure: "true"

  # 明确 ArgoCD 运行在 /argocd 子路径
  server.rootpath: "/argocd"
  server.basehref: "/argocd"
