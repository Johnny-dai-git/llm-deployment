# DCGM Exporter Helm Values
runtimeClassName: nvidia

serviceMonitor:
  enabled: true
  interval: 15s

resources:
  requests:
    memory: 1Gi
  limits:
    memory: 1Gi

# Node Selector（匹配当前配置）
nodeSelector:
  system: "true"
  gpu-node: "true"
